{
  "name": "cf-ai-chatapp-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for the AI Chat Application",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.2"
  }
}
